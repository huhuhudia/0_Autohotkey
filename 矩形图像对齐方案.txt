A = [(width, posX), (width1,posX1), (width2,posX2)]

边沿间距对齐：


中心间距对齐：
以最小（最左）x坐标图像为起始点，
最右侧（x+width）值最大的图像元素为基准

	排序所有元素的左x值，取最小为起始点
	排序所有元素的(x + width)值,取最大为结束点
	以上两元素位置不变,记录两元素的中心x坐标（x + （w/ 2）

	其他排列元素：
	以左侧x坐标为基准升序排列
	除起始点与终点元素,此类元素应做坐标移动

	中心间距= (结x - 起x) / (总元素数量 - 1)
	按顺序排列，序号（1起） * 中心间距 - （w/2）
	

	



若起始点与结束点图像为同一元素，在该图像元素内部排列其他元素

